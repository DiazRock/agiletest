import { CityBlankSpaceStyle, IconSun, TextContainer } from './styles'

export const CityBlankSpace = () => {
  return (
    <CityBlankSpaceStyle>
      <IconSun />
      <TextContainer> Submit a city to fill up this space </TextContainer>
    </CityBlankSpaceStyle>
  )
}
